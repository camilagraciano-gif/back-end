// verifica se LOGIN e senha
// corresponde ao mesmo tempo ( && )

const login = "superman"
const senha = "comida123"

if (login == "superman" && senha == 'segredo123') {
    console. log('acesso permitido!')
} else {
    console.warn('verifique seus dados')
}
